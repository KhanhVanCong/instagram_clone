<%= turbo_stream.remove("post#{@comment.post_id}ModalComment#{@comment.id}") %>

<%= turbo_stream.remove("comment#{@comment.id}") %>

<% if @post.comments.size > 0 %>
	<%= turbo_stream.update("post#{@post.id}comments_count", "View all #{pluralize(@post.comments.size, "comment")}") %>
<% else %>
	<%= turbo_stream.remove("post#{@post.id}comments_count") %>
<% end %>
